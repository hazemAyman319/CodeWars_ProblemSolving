#include <iostream>
#include <bits\stdc++.h>
using namespace std;
#define ll long long
const int N = 1e5;
int n, x;
vector<int> V;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin>>n;
    for(int i=0; i<n; i++){
        cin>>x;
        v.push_back(x);
    }
    sort(v.begin() , v.end());
    for(int i=0; i<n; i++)
    {
        cout<<v[i]<<endl;
    }
    for(int x : v){
        cout << x <<endl;
    }
    return 0;
}
